import styled from "styled-components";

export const StyledLayout = styled.div`
  width: 100vw;
  height: 100vh;

  display: flex;
  flex-direction: column;
`;

export const StyledMain = styled.main`
  background-color: hsl(105, 0%, 90%);

  flex-grow: 1;
`;
